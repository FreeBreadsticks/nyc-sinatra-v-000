<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit a Figure</title>
  </head>
  <body>
    <h1>Edit Figure</h1>
    <form class="" action="/figures/<%= @figure.id %>" method="post">
      
      <label>Name:</label>
      <p><input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>"></p>
      <br>

      <label>Titles</label>
      <% Title.all.each do |title|%>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%=title.id%>" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title) %> > <%= title.name %> </input>
      <% end %>
      <br>

      <label>Add New Landmark</label>
      <input type="text" name="landmark[name]" id="new_landmark" value="">
      <br>

      <input type="submit" name="" value="Edit Figure">
    </form>
  </body>
</html>
